<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Attendance Portal</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        :root { --primary: #2c3e50; --accent: #3498db; --success: #27ae60; --danger: #e74c3c; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #f0f2f5; margin: 0; padding: 20px; }
        .container { max-width: 1000px; margin: 0 auto; }
        .dashboard-header { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); margin-bottom: 25px; text-align: center; }
        
        .stats-container { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-bottom: 30px; }
        .stat-card { background: white; padding: 20px; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); text-align: center; border-bottom: 4px solid var(--accent); }
        .stat-card h3 { margin: 0; color: #7f8c8d; font-size: 14px; text-transform: uppercase; }
        .stat-card .value { font-size: 32px; font-weight: bold; color: var(--primary); margin: 10px 0; }

        .attendance-card { background: white; padding: 25px; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { background-color: #f8f9fa; color: var(--primary); font-weight: 600; text-align: left; padding: 15px; border-bottom: 2px solid #eee; }
        td { padding: 15px; border-bottom: 1px solid #eee; color: #2c3e50; }
        
        .status-pill { padding: 6px 12px; border-radius: 20px; font-size: 12px; font-weight: bold; text-transform: uppercase; }
        .status-Present { background: #d4edda; color: #155724; }
        .status-Absent { background: #f8d7da; color: #721c24; }
        
        .logout-btn { color: var(--danger); text-decoration: none; font-weight: bold; float: right; }
    </style>
</head>
<body>

<div class="container">
    <div class="dashboard-header">
        <a href="/logout" class="logout-btn">Logout</a>
        <h1>My Attendance Portal</h1>
        <p>Welcome, <strong><%= user.name %></strong> | Roll No: <strong><%= user.rollNo %></strong></p>
    </div>

    <div class="stats-container">
        <div class="stat-card">
            <h3>Total Classes</h3>
            <div class="value"><%= totalCount %></div>
        </div>
        <div class="stat-card" style="border-bottom-color: var(--success);">
            <h3>Present</h3>
            <div class="value" style="color: var(--success);"><%= presentCount %></div>
        </div>
        <div class="stat-card" style="border-bottom-color: var(--danger);">
            <h3>Absent</h3>
            <div class="value" style="color: var(--danger);"><%= totalCount - presentCount %></div>
        </div>
        <div class="stat-card">
            <h3>Attendance %</h3>
            <div class="value" style="color: var(--accent);">
                <%= totalCount > 0 ? ((presentCount / totalCount) * 100).toFixed(1) : 0 %>%
            </div>
        </div>
    </div>

    <div class="attendance-card">
        <h3>Attendance History</h3>
        <table>
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Time Slot</th>
                    <th>Subject</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                <% if (records && records.length > 0) { %>
                    <% records.forEach(rec => { 
                        // Find this specific student's status within the attendance array
                        const myEntry = rec.students.find(s => s.studentId === user.rollNo);
                    %>
                        <tr>
                            <td><%= rec.date %></td>
                            <td><%= rec.manualTime || 'N/A' %></td>
                            <td><%= rec.subject %></td>
                            <td>
                                <% if (myEntry) { %>
                                    <span class="status-pill status-<%= myEntry.status %>">
                                        <%= myEntry.status %>
                                    </span>
                                <% } else { %>
                                    <span class="status-pill" style="background:#eee;">Not Marked</span>
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="4" style="text-align: center; padding: 40px; color: #7f8c8d;">
                            No attendance records found for Roll No: <%= user.rollNo %>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</div>

</body>
</html>